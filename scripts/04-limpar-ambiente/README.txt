╔═══════════════════════════════════════════════════════════════════════════╗
║              🧹 LIMPAR AMBIENTE - NEXT.JS                                 ║
╚═══════════════════════════════════════════════════════════════════════════╝

📋 DESCRIÇÃO
═══════════════════════════════════════════════════════════════════════════

Scripts para limpar cache e arquivos temporários do Next.js.

✅ Limpar cache (.next, node_modules/.cache)
✅ Limpar coverage
✅ Limpeza completa opcional

🚀 COMO USAR
═══════════════════════════════════════════════════════════════════════════

Windows - Duplo clique:
   ✨ limpar.bat  (Limpa cache padrão)

PowerShell:
   .\limpar.ps1          (Limpa cache padrão)
   .\limpar.ps1 -Cache   (Limpa cache)
   .\limpar.ps1 -All     (Limpeza completa)

Linux/Mac:
   ./limpar.sh           (Limpa cache padrão)
   ./limpar.sh --cache   (Limpa cache)
   ./limpar.sh --all     (Limpeza completa)

📊 O QUE CADA OPÇÃO FAZ
═══════════════════════════════════════════════════════════════════════════

Padrão (sem flags):
  • Remove .next/
  • Remove node_modules/.cache/

--Cache ou -Cache:
  • Remove .next/
  • Remove node_modules/.cache/

--All ou -All:
  • Remove .next/
  • Remove node_modules/.cache/
  • Remove coverage/
  • Remove .turbo/

💡 QUANDO USAR
═══════════════════════════════════════════════════════════════════════════

✨ Cache padrão:
   • Após mudanças significativas no código
   • Quando build está com problemas
   • Antes de commits importantes

✨ Limpeza completa:
   • Reset completo do ambiente
   • Antes de deploy
   • Quando há problemas persistentes

════════════════════════════════════════════════════════════════════════════

Criado com ❤️ para facilitar o desenvolvimento! 🚀

