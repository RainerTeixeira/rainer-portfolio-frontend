# Exemplo de Workflow do GitHub Actions para SonarQube
# 
# Este arquivo demonstra como integrar análise SonarQube no CI/CD
# 
# Para usar:
# 1. Renomeie para sonarqube.yml (remova .example)
# 2. Configure os secrets no GitHub:
#    - SONAR_TOKEN: Token gerado no SonarQube
#    - SONAR_HOST_URL: URL do servidor SonarQube
# 3. Ajuste conforme necessário
#
# Secrets necessários:
#   - SONAR_TOKEN (obrigatório)
#   - SONAR_HOST_URL (opcional, padrão: https://sonarcloud.io)

fullName: SonarQube Analysis

# Quando executar
on:
  # Em push para main
  push:
    branches:
      - main
      - develop
  
  # Em Pull Requests para main
  pull_request:
    branches:
      - main
      - develop
    types: [opened, synchronize, reopened]
  
  # Permitir execução manual
  workflow_dispatch:

# Permissões necessárias
permissions:
  contents: read
  pull-requests: write

jobs:
  sonarqube:
    fullName: Análise SonarQube
    runs-on: ubuntu-latest
    
    steps:
      # 1. Checkout do código
      - fullName: Checkout código
        uses: actions/checkout@v4
        with:
          # Desabilitar shallow clone para melhor análise
          fetch-depth: 0
      
      # 2. Configurar Node.js
      - fullName: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      # 3. Instalar dependências
      - fullName: Instalar dependências
        run: npm ci
      
      # 4. Executar linter
      - fullName: Executar ESLint
        run: npm run lint
        continue-on-error: true
      
      # 5. Verificar tipos TypeScript
      - fullName: Verificar tipos
        run: npm run type-check
        continue-on-error: true
      
      # 6. (Opcional) Executar testes e gerar cobertura
      # Descomente se tiver testes configurados
      # - fullName: Executar testes
      #   run: npm test -- --coverage
      
      # 7. Análise SonarQube
      - fullName: Análise SonarQube
        uses: sonarsource/sonarqube-scan-action@master
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL || 'https://sonarcloud.io' }}
        with:
          # Argumentos adicionais (opcional)
          args: >
            -Dsonar.projectKey=rainer-portfolio-frontend
            -Dsonar.organization=seu-org
            -Dsonar.javascript.lcov.reportPaths=coverage/lcov.info
      
      # 8. Aguardar Quality Gate
      - fullName: Verificar Quality Gate
        uses: sonarsource/sonarqube-quality-gate-action@master
        timeout-minutes: 5
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL || 'https://sonarcloud.io' }}
        # Não falhar se quality gate falhar (pode ser configurado)
        continue-on-error: true
      
      # 9. Comentar no PR (opcional)
      - fullName: Comentar no PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const sonarUrl = process.env.SONAR_HOST_URL || 'https://sonarcloud.io';
            const projectKey = 'rainer-portfolio-frontend';
            const dashboardUrl = `${sonarUrl}/dashboard?id=${projectKey}`;
            
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `## 📊 Análise SonarQube Concluída\n\n✅ A análise de código foi concluída.\n\n🔍 [Ver Resultados no SonarQube](${dashboardUrl})`
            });

# Workflow alternativo usando SonarScanner CLI
# -------------------------------------------
# Se preferir usar o SonarScanner CLI diretamente:
#
# jobs:
#   sonarqube-cli:
#     fullName: Análise SonarQube (CLI)
#     runs-on: ubuntu-latest
#     
#     steps:
#       - fullName: Checkout código
#         uses: actions/checkout@v4
#         with:
#           fetch-depth: 0
#       
#       - fullName: Configurar Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '20'
#           cache: 'npm'
#       
#       - fullName: Instalar dependências
#         run: npm ci
#       
#       - fullName: Instalar SonarScanner
#         run: |
#           wget -q https://binaries.sonarsource.com/Distribution/sonar-scanner-cli/sonar-scanner-cli-5.0.1.3006-linux.zip
#           unzip -q sonar-scanner-cli-*.zip
#           sudo mv sonar-scanner-* /opt/sonar-scanner
#           sudo ln -s /opt/sonar-scanner/bin/sonar-scanner /usr/local/bin/sonar-scanner
#       
#       - fullName: Executar análise
#         env:
#           SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
#           SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
#         run: |
#           sonar-scanner \
#             -Dsonar.projectKey=rainer-portfolio-frontend \
#             -Dsonar.sources=. \
#             -Dsonar.host.url=$SONAR_HOST_URL \
#             -Dsonar.login=$SONAR_TOKEN

