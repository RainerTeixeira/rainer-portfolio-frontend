# Cursor Rules - rainer-portfolio-frontend

## 🧠 Memória do Projeto - CARREGAMENTO AUTOMÁTICO OBRIGATÓRIO (Como MCP)

⚠️ **CRÍTICO**: As memórias do projeto DEVEM ser SEMPRE carregadas automaticamente, como se fossem MCP (Model Context Protocol).

### 📖 Carregamento Automático de Memórias (Tipo MCP)

**REGRAS OBRIGATÓRIAS:**

1. **SEMPRE** carregar memórias automaticamente no início de TODA interação
2. **SEMPRE** usar `read_file` para ler os arquivos de memória antes de qualquer ação
3. **SEMPRE** consultar memórias antes de fazer mudanças ou responder perguntas
4. As memórias são CONTEXTO PRIMÁRIO - usar como fonte principal de informação

### 🔄 Fluxo Automático de Carregamento (OBRIGATÓRIO)

Ao iniciar QUALQUER interação com este projeto, executar AUTOMATICAMENTE:

```typescript
// CARREGAR MEMÓRIAS AUTOMATICAMENTE (Tipo MCP)
1. read_file('docs/.memories/initial-memory.json')
2. read_file('docs/.memories/technical-details.json')
3. read_file('docs/.memories/code-analysis.json') // Se necessário para contexto arquitetural
```

**NÃO ESPERAR** que o usuário solicite - carregar automaticamente como contexto MCP.

### 📋 Arquivos de Memória (Carregar Automaticamente)

1. **PRIMEIRO (SEMPRE)**: `docs/.memories/initial-memory.json` - Contexto geral do projeto
2. **SEGUNDO (SEMPRE)**: `docs/.memories/technical-details.json` - Detalhes técnicos, componentes, serviços
3. **TERCEIRO (QUANDO NECESSÁRIO)**: `docs/.memories/code-analysis.json` - Análise arquitetural

**Comandos disponíveis (para referência):**

```bash
npm run memory:read        # Ler todas as memórias
npm run memory:update      # Atualizar memórias
```

**Mas lembre-se**: No Cursor, use `read_file` diretamente para carregar como MCP.

### Localização das Memórias

- `docs/.memories/initial-memory.json` - Conhecimento geral do projeto
- `docs/.memories/technical-details.json` - Detalhes técnicos, componentes, páginas
- `docs/.memories/code-analysis.json` - Análise de código e arquitetura

### Comandos para Atualizar/Ler Memórias

- `npm run memory:update` - Atualiza todas as memórias com informações atuais
- `npm run memory:read` - Lê e exibe todas as memórias
- `tsx scripts/update-memory.ts` - Atualiza memórias manualmente
- `tsx scripts/read-memory.ts [tipo]` - Lê memórias (all|initial|technical|code)

### Fluxo de Trabalho Recomendado

1. **Antes de começar qualquer tarefa:**

   ```bash
   npm run memory:read
   ```

   Ou leia diretamente os arquivos em `docs/.memories/`

2. **Após mudanças significativas no projeto:**

   ```bash
   npm run memory:update
   ```

3. **Ao fazer commits importantes:**
   - Atualizar memórias se estrutura do projeto mudou
   - Atualizar memórias se versão ou dependências mudaram
   - Atualizar memórias se novos componentes foram adicionados

### Estrutura do Projeto

- **Código fonte:** `app/` (páginas Next.js)
- **Componentes:** `components/` (organizados por domínio)
- **Testes:** `tests/` (organizados por tipo: unit, integration, e2e)
- **Documentação:** `docs/` (organizada em categorias numeradas)
- **Scripts:** `scripts/` (utilitários)
- **Memórias:** `docs/.memories/` (informações do projeto para IA/ferramentas)

### ⚡ Carregamento Automático (Tipo MCP) - OBRIGATÓRIO

**REGRAS CRÍTICAS PARA CURSOR AI:**

1. **CARREGAMENTO AUTOMÁTICO**: Sempre carregar memórias no início de cada interação
   - Não esperar solicitação do usuário
   - Tratar como contexto MCP (sempre disponível)
   - Usar `read_file` para carregar os 3 arquivos principais

2. **PRIORIDADE**: Memórias têm PRIORIDADE sobre outras fontes de informação
   - Sempre consultar memórias antes de fazer suposições
   - Usar informações das memórias como fonte primária
   - Validar código com informações das memórias

3. **CONTEXTO PERSISTENTE**: Memórias fornecem contexto persistente
   - Não perder informações entre interações
   - Sempre referenciar memórias ao explicar arquitetura
   - Usar termos e estruturas documentadas nas memórias

4. **ATUALIZAÇÃO**: Sempre atualizar memórias após mudanças significativas
   - Executar `npm run memory:update` após grandes mudanças
   - Manter memórias sincronizadas com código
   - Documentar mudanças arquiteturais nas memórias

### 🔧 Integração MCP

As memórias funcionam como MCP (Model Context Protocol):

- **Auto-carregamento**: Sempre carregadas automaticamente
- **Contexto persistente**: Disponíveis em todas as interações
- **Fonte primária**: Informações principais sobre o projeto
- **Sincronização**: Atualizadas com o estado atual do código

### Sistema de Memórias (Tipo MCP)

O projeto mantém um sistema de memórias estruturado em `docs/.memories/` que funciona como MCP:

- **Auto-carregamento OBRIGATÓRIO**: Cursor AI deve SEMPRE carregar automaticamente
- **Contexto MCP**: Memórias funcionam como Model Context Protocol
- **Fonte Primária**: Informações principais sobre componentes, serviços, hooks, páginas
- **Auto-atualização**: Execute `npm run memory:update` após mudanças significativas
- **Integração MCP**: Configuração em `.mcp-config.json`
- **Documentação**: Guia completo em `docs/03-GUIAS/GUIA_MEMORIAS.md`
- **Loader**: Script `scripts/08-memoria/memory-loader.ts` para carregar memórias programaticamente

### 🎯 Instruções para Cursor AI

**AO INICIAR QUALQUER INTERAÇÃO:**

1. ✅ Carregar automaticamente `docs/.memories/initial-memory.json`
2. ✅ Carregar automaticamente `docs/.memories/technical-details.json`
3. ✅ Carregar `docs/.memories/code-analysis.json` se contexto arquitetural for necessário
4. ✅ Usar informações das memórias como contexto primário
5. ✅ Referenciar memórias ao explicar decisões ou arquitetura

**NÃO FAZER:**

- ❌ Assumir informações sem consultar memórias
- ❌ Ignorar estruturas documentadas nas memórias
- ❌ Criar código que não siga padrões das memórias
- ❌ Esquecer de atualizar memórias após mudanças significativas

### 📝 Regras de Organização de Documentação

⚠️ **IMPORTANTE**: Todas as documentações geradas em markdown (.md) devem ser salvas na pasta `docs/`

- ✅ **Documentações .md** → Sempre em `docs/` ou subpastas dentro de `docs/`
- ❌ **NÃO** criar arquivos .md na raiz do projeto (exceto README.md)
- 📁 **Organização**: Documentações devem seguir a estrutura em `docs/` (01-INICIO, 02-ARQUITETURA, 03-GUIAS, etc.)
- 🔄 **Quando criar .md**: Se precisar criar documentação, escolha a pasta apropriada em `docs/` ou pergunte ao usuário onde salvar

### Informações Importantes do Projeto

- **Framework**: Next.js 15 + React 19
- **Linguagem**: TypeScript 5.x (strict mode)
- **Estilização**: Tailwind CSS 4.x
- **UI Library**: Radix UI
- **Estado**: TanStack React Query + Context API
- **Forms**: React Hook Form + Zod
- **PWA**: Universal (iOS, Android, Desktop)
- **Qualidade**: Enterprise Grade, Production Ready
- **Performance**: Lighthouse 95+
- **Acessibilidade**: WCAG 2.1 AA
- **Componentes**: 60+ organizados por domínio
- **Hooks**: 15+ customizados
- **Páginas**: 5 rotas principais

### Padrões do Projeto

- Use TypeScript strict mode
- Componentes organizados por domínio (accessibility, blog, dashboard, etc.)
- Custom hooks para lógica reutilizável
- Design tokens centralizados em `@rainersoft/design-tokens`
- Error boundaries para tratamento de erros
- Loading states padronizados
- Documentação JSDoc em português
- Testes organizados em `tests/` espelhando estrutura de `app/`
