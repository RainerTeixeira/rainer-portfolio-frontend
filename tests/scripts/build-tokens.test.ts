/**
 * Placeholder para os testes de build de tokens.
 *
 * O arquivo original vive em `rainer-design-tokens/tests/scripts/build-tokens.test.ts`
 * e depende do pipeline de geração de tokens daquele pacote.
 *
 * Mantemos este stub aqui para que o Jest não falhe por referência
 * ausente e para documentar a origem do teste real.
 */

describe.skip('build-tokens.ts (rainer-design-tokens)', () => {
  it('testes reais residem no pacote de tokens; execute lá para validar o build', () => {
    expect(true).toBe(true);
  });
});
