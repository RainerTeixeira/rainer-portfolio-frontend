/**
 * Testes para DashboardService - Analytics
 *
 * @deprecated Este arquivo foi movido para tests/lib/api/services/dashboard.service.test.ts
 * Mantido apenas para referência. Os testes devem ser executados no novo local.
 */

// Testes movidos para: tests/lib/api/services/dashboard.service.test.ts
describe('DashboardService - Analytics (deprecated)', () => {
  it('testes movidos para tests/lib/api/services/dashboard.service.test.ts', () => {
    expect(true).toBe(true);
  });
});
